import { Button } from '@/components/ui/button';

export default function ButtonPage({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div>
      <Button>11</Button>
    </div>
  );
}
